# 微信小程序用户文案审计报告

## 已修改

- 清理用户界面中的开发者视角文案：移除了“后端返回”“读取真实订单数据”“真实方案/真实服务/真实履约”等开发期对照 mock 的表达，改为“暂无待处理事项”“暂无待履约订单”“加载中…”等终端用户视角文案。
- 统一替换页面 toast、错误状态和空状态中直接展示 `error.message` 的逻辑，改为通过 `friendlyErrorMessage(error, fallback)` 输出短、自然、无技术词的错误提示。
- 将服务层请求超时提示从“开发者工具代理设置”改为“请求超时，请检查网络连接后重试”。
- 清理额外发现的开发期文案：支付页“开发构建/联调/模拟支付”、登录与设置页“开发模式/开发诊断”、入驻页“后台审核/假订单/假收入”、履约页“订单未返回坐标/无人机ID”、以及用户可见的“响应”类状态文案。

## 已新增

- 新增 `mini-program/src/utils/errorMessage.ts`，提供全局 `friendlyErrorMessage(error, fallback)` 工具。
- 迁移 `airspace/no-fly` 页面本地错误文案函数到全局工具，并复用到小程序各主要错误展示入口。

## 主动扫描发现的额外问题清单

- 已修复支付页开发期文案，同时保留 `mock` 支付方式 key，不改枚举和业务流程。
- 已修复“缺少订单ID”“未返回文件地址/订单ID”等用户可见提示，改为“缺少订单信息”“请稍后在订单列表查看”等自然表达。
- 已修复“开发中”类占位提示，改为“暂未开放”等终端用户表达。
- 已确认保留实名认证语境中的“真实姓名”；注释、console 日志、className、资源文件名、请求字段名和接口路径未纳入本次用户文案改写。
- 待人工确认：无。
